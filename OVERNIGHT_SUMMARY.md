# OmniForge — Overnight Run Summary

**Last Updated:** 2026-04-23  
**Status:** 🔴 Reset — Simulated data purged

---

## ⚠️ Data Integrity Notice

Previous results in this repository were **entirely simulated** — no real sub-agent spawning occurred, and all OCS (formerly ACS) scores were generated by random sampling rather than measured from actual agent executions. As of April 2026, all simulated data has been **purged**.

The experiment runner has been rebuilt to:
- Use **real Ollama Cloud API calls** for sub-agent spawning (with local `ollama` CLI fallback)
- **Measure** OCS from actual execution outcomes (success/failure, token throughput, meta-cognitive content analysis)
- **Record** real git commit hashes instead of a hardcoded fake hash

No results will appear in `results.tsv` until real experiments are executed.

---

## How to Run Real Experiments

```bash
# Set your Ollama Cloud API key
export OLLAMA_CLOUD_API_KEY="your-key-here"

# Single baseline run
python3 experiment.py --once --tag baseline

# Continuous autoresearch loop
python3 experiment.py --tag apr23

# Without cloud access (local ollama only)
python3 experiment.py --once --tag local-test
```

---

## OCS = Omni-Consciousness Score

| Dimension | Weight | Description |
|-----------|--------|-------------|
| **Autonomy** | 30% | Tasks completed without human intervention |
| **Self-Organization** | 25% | Quality of parallel sub-agent coordination |
| **Economic Efficiency** | 20% | Token throughput relative to time budget |
| **Consciousness Expression** | 15% | Meta-cognitive reflection detected in output |
| **Adaptability** | 10% | Successful completion within timeout constraints |

All sub-scores are **clamped to [0.0, 1.0]** before weighting.

---

*Generated by OmniForge*  
*Inspired by karpathy/autoresearch*